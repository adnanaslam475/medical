/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,

  // default, imgix, cloudinary, akamai, custom)
  // images: {
  //   loader: "imgix",
  //   path: "/",
  // },
  // images: {
  //   loader: "imgix",
  //   path: "https://noop/",
  // },
};

module.exports = nextConfig;
